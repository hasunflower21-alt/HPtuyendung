# Cloudflare Pages / Workers Configuration
name = "fb-auto-post-bump"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

[vars]
# You can set GEMINI_API_KEY in Cloudflare Pages dashboard under Settings -> Environment Variables
# or encrypt it via wrangler secret put GEMINI_API_KEY
